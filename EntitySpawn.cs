using Godot;

public class EntitySpawn : Node
{
    public override void _Ready()
    {
        //var player = GD.Load<PackedScene>("res://player.tscn");
        //var node = player.Instance();
        //var kb = (KinematicBody2D)node;
        //var ySort = FindNode("YSort");
        //kb.Position = new Vector2(300, 300);
        //ySort.AddChild(node);
    }
}
